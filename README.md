# 線上工具集

免費的線上工具集合，提供 PDF、圖片、文件處理等多種實用工具。所有處理都在瀏覽器本地完成，確保您的隱私和數據安全。

## 🚀 功能特色

### 📄 PDF 工具
- **PDF 轉 Word** - 將 PDF 文件轉換為可編輯的 Word 文檔
- **PDF 轉圖片** - 將 PDF 的每一頁轉換為 PNG 圖片
- **合併 PDF** - 將多個 PDF 文件合併為一個
- **壓縮 PDF** - 減少 PDF 文件大小

### 🖼️ 圖片工具
- **圖片壓縮** - 壓縮圖片大小，同時保持良好畫質
- **圖片轉檔** - 轉換圖片格式（JPG, PNG, WebP 等）
- **圖片裁切** - 裁切和調整圖片尺寸

### 📝 文件工具
- **Word 轉 PDF** - 將 Word 文檔轉換為 PDF 格式
- **Excel 轉 CSV** - 將 Excel 文件轉換為 CSV 格式

### 🛠️ 其他工具
- **JSON 格式化** - 格式化、壓縮和驗證 JSON 數據
- **Base64 編碼/解碼** - Base64 編碼和解碼工具
- **QR Code 生成器** - 生成 QR Code 二維碼

## 🛡️ 隱私保護

- ✅ 所有處理都在瀏覽器本地完成
- ✅ 文件不會上傳到任何伺服器
- ✅ 無需註冊或登入
- ✅ 完全免費使用

## 💻 技術棧

- **前端框架**: React 19 + TypeScript
- **建置工具**: Vite 7
- **樣式**: Tailwind CSS 3
- **路由**: React Router v7
- **PDF 處理**: pdf-lib, pdfjs-dist
- **圖片處理**: browser-image-compression, html2canvas
- **其他**: QRCode.js

## 🚀 開始使用

### 安裝依賴

```bash
npm install
```

### 開發模式

```bash
npm run dev
```

在瀏覽器中打開 http://localhost:5173

### 建置專案

```bash
npm run build
```

### 預覽建置結果

```bash
npm run preview
```

## 📦 部署到 GitHub Pages

### 方法 1: 使用 npm 腳本（手動部署）

```bash
npm run deploy
```

### 方法 2: 使用 GitHub Actions（自動部署）

專案已配置 GitHub Actions，當您推送到 `main` 分支時會自動部署。

#### 設定步驟：

1. 在 GitHub 倉庫設定中啟用 GitHub Pages
2. 前往 **Settings** → **Pages**
3. 在 **Source** 選擇 **GitHub Actions**
4. 推送代碼到 `main` 分支

```bash
git add .
git commit -m "Initial commit"
git push origin main
```

5. 等待 Actions 完成部署
6. 訪問 `https://[your-username].github.io/tony-1/`

## ⚙️ 配置

### 修改基礎路徑

如果您的倉庫名稱不是 `tony-1`，請修改：

1. `vite.config.js` 中的 `base` 選項
2. `src/main.jsx` 中的 `basename` 屬性

```javascript
// vite.config.js
base: '/your-repo-name/'

// src/main.jsx
<BrowserRouter basename="/your-repo-name">
```

## 💰 Google AdSense 整合

專案已整合 Google AdSense 支援。詳細設定步驟請查看 [ADSENSE_SETUP.md](./ADSENSE_SETUP.md)

### 快速設定：

1. 申請 Google AdSense 帳號
2. 取得您的 Publisher ID (`ca-pub-XXXXXXXXXX`)
3. 修改以下檔案：
   - `index.html`
   - `src/components/AdBanner.jsx`
4. 替換 `ca-pub-XXXXXXXXXX` 為您的實際 ID

## 📁 專案結構

```
tony-1/
├── .github/
│   └── workflows/
│       └── deploy.yml          # GitHub Actions 部署配置
├── public/                     # 靜態資源
├── src/
│   ├── components/
│   │   ├── Navigation.jsx      # 導航組件
│   │   └── AdBanner.jsx        # 廣告橫幅組件
│   ├── pages/
│   │   ├── Home.jsx            # 首頁
│   │   ├── pdf-tools/          # PDF 工具頁面
│   │   ├── image-tools/        # 圖片工具頁面
│   │   ├── document-tools/     # 文件工具頁面
│   │   └── other-tools/        # 其他工具頁面
│   ├── App.jsx                 # 主應用組件
│   ├── main.jsx                # 入口文件
│   └── index.css               # 全局樣式
├── index.html                  # HTML 模板
├── vite.config.js              # Vite 配置
└── package.json                # 專案配置
```

## 🔧 開發說明

### 添加新工具

1. 在相應的目錄下創建新組件（例如：`src/pages/pdf-tools/NewTool.jsx`）
2. 在 `src/App.jsx` 中添加路由
3. 在 `src/pages/Home.jsx` 中添加工具卡片

### 自定義樣式

- 全局樣式：修改 `src/index.css`
- 組件樣式：在組件文件中使用 inline styles 或創建對應的 CSS 文件

## 📄 授權

MIT License

## 🤝 貢獻

歡迎提交 Issues 和 Pull Requests！

## 📧 聯繫方式

如有問題或建議，請開 Issue。

## ⭐ Star History

如果這個專案對您有幫助，請給個 Star ⭐

---

**注意**: 某些功能（如 PDF 轉 Word、Word 轉 PDF）需要後端服務支援，目前僅提供前端界面和說明。
